# Продолжение прерванного обучения

## Что происходит при прерывании обучения

Если вы прервете обучение на 11-й эпохе из 100:

### ✅ Что сохраняется:

1. **Последний чекпоинт** (`last.pt`) - сохранен автоматически YOLOv8
   - Содержит состояние модели на момент прерывания
   - Включает веса модели, состояние оптимизатора, номер эпохи

2. **Лучший чекпоинт** (`best.pt`) - модель с лучшей метрикой до момента прерывания
   - Автоматически обновляется при улучшении метрик

3. **Периодические чекпоинты** - согласно `save_period` в конфиге
   - При `save_period: 10` будут сохранены чекпоинты на эпохах 10, 20, 30 и т.д.
   - На 11-й эпохе будет доступен чекпоинт с 10-й эпохи

4. **Логи и метрики** - все графики и метрики сохраняются в директории результатов

### 📁 Где находятся файлы:

Все файлы сохраняются в:
```
runs/train/coco_person_yolov8/
├── weights/
│   ├── last.pt          # Последний чекпоинт (11-я эпоха)
│   ├── best.pt          # Лучший чекпоинт (до 11-й эпохи)
│   └── epoch10.pt       # Чекпоинт на 10-й эпохе (если save_period=10)
├── results.png          # Графики метрик
├── confusion_matrix.png # Матрица ошибок
└── args.yaml            # Сохраненные параметры обучения
```

## Как продолжить обучение

### Способ 1: Автоматическое продолжение (рекомендуется)

YOLOv8 автоматически продолжит обучение, если вы запустите тот же скрипт с теми же параметрами:

```bash
py src/train.py --config configs/train_config.yaml
```

YOLOv8 обнаружит существующую директорию и автоматически загрузит `last.pt` для продолжения обучения.

### Способ 2: Явное указание чекпоинта

Если нужно продолжить с конкретного чекпоинта:

```python
from ultralytics import YOLO

# Загрузите чекпоинт
model = YOLO('runs/train/coco_person_yolov8/weights/last.pt')

# Продолжите обучение
model.train(
    data='data/coco/yolo_format/dataset.yaml',
    epochs=100,  # Общее количество эпох (продолжит с текущей)
    resume=True  # Важно: resume=True для продолжения
)
```

### Способ 3: Использование скрипта с параметром resume

Добавьте параметр `--resume` в скрипт обучения (см. ниже).

## Важные моменты

1. **Номер эпохи**: Обучение продолжит с того места, где остановилось (11-я эпоха)
2. **Метрики**: Все метрики будут продолжены, графики обновятся
3. **Лучшая модель**: `best.pt` будет обновляться только если метрики улучшатся
4. **Параметры**: Используются те же параметры, что были сохранены в `args.yaml`

## Примеры

### Продолжить с последнего чекпоинта:
```bash
py src/train.py --config configs/train_config.yaml --resume
```

### Продолжить с конкретного чекпоинта:
```bash
py src/train.py --config configs/train_config.yaml --resume runs/train/coco_person_yolov8/weights/epoch10.pt
```

### Использовать лучший чекпоинт для инференса:
```bash
py src/inference.py --video data/videos/test.mp4 --model runs/train/coco_person_yolov8/weights/best.pt
```

## Рекомендации

- **Не удаляйте** директорию `runs/train/coco_person_yolov8/` - там хранятся все чекпоинты
- **Регулярно проверяйте** метрики в `results.png` - если модель не улучшается, можно остановить обучение
- **Сохраняйте копии** лучших чекпоинтов в отдельную папку для безопасности
- При `save_period: 10` чекпоинты сохраняются каждые 10 эпох - это безопасный интервал

